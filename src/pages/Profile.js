import React, { useState } from "react";
import Layout from "../shared/Layout";

import { Button } from "../components/Button";
const Profile = () => {
  return (
    <Layout>
      <h1>Profile Page</h1>
      <div className="flex flex-col items-center gap-10 mt-5 w-full h-full"></div>
    </Layout>
  );
};

export default Profile;
